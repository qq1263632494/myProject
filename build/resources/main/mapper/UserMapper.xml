<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mapper.UserMapper">
    <resultMap id="simpleMap" type="User">
        <id property="id" column="id"/>
        <collection property="id" javaType="ArrayList" column="id" ofType="User"/>
    </resultMap>
    <insert id="save" parameterType="User" useGeneratedKeys="true">
        INSERT INTO TB_USER(name, sex, age) VALUES (#{name}, #{sex}, #{age})
    </insert>
    <update id="update">
        UPDATE tb_user SET tb_user.name = #{username}, sex=#{sex}, age=#{age} WHERE id = #{id}
    </update>
    <delete id="delete">
        delete from tb_user where ID = #{id}
    </delete>
    <select id="selectAll" resultMap="simpleMap">
        select * from tb_user
    </select>
</mapper>